import React, { useState } from 'react';

const VehicleViewer = () => {
    [vehicles, setVehicles] = useState([]);
    
    return (
        <>
            <h1>The Vehicle Viewer</h1>
        </>
    );
};